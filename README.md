# Actuarial Vocabulary

A minimal GitHub Pages flashcard app for life actuarial vocabulary.

The app is static: open `index.html` through a local server or deploy the repository with GitHub Pages. Vocabulary data lives in `data/terms.json`, and review feedback is stored locally in the browser.
